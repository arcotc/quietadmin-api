package uk.co.quietadmin.web.error;

public enum ErrorCode {
    VALIDATION_ERROR,
    BAD_REQUEST,
    UNAUTHORIZED,
    FORBIDDEN,
    TOKEN_EXPIRED,
    TOKEN_INVALID,
    INTERNAL_ERROR,
    EMAIL_NOT_VERIFIED
}