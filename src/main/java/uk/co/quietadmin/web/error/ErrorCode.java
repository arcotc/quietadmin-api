package uk.co.quietadmin.web.error;

public enum ErrorCode {
    VALIDATION_ERROR,
    BAD_REQUEST,
    UNAUTHORIZED,
    FORBIDDEN,
    TOKEN_EXPIRED,
    TOKEN_INVALID,
    INTERNAL_ERROR,
    TOO_MANY_REQUESTS,
    EMAIL_NOT_VERIFIED
}